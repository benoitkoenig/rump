import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 120,
    marginHorizontal: 60,
    textAlign: 'center',
  },

  query: {
    fontSize: 16,
    color: '#fafafa',
    textAlign: 'center',
  },

  link: {
    fontSize: 16,
    marginTop: 24,
    textDecorationLine: 'underline',
    color: '#00d0c0',
    textAlign: 'center',
  },
});
