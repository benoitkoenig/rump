import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    width: '100%',
    backgroundColor: '#00d0c0',
  },

  title: {
    color: '#fafafa',
    marginTop: 24,
    marginBottom: 24,
    fontSize: 24,
    textAlign: 'center',
  },
});